spring.application.name=app

# Railway injects PORT automatically; falls back to 8080 locally
server.port=${PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://interchange.proxy.rlwy.net:20246/railway?sslmode=require&ssl=true&connectTimeout=30&socketTimeout=60}

spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:leFAvnbRyGeqXBezNlhgIdvoNBoxyGHB}

# HikariCP connection pool timeout
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.maximum-pool-size=5

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Razorpay
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_TXDBWLptRpBdn3}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:ltG4AD609lP40LRVgk7j7Fgb}

# Allow the Netlify frontend to call this API directly if you ever bypass the Netlify proxy
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5500}
